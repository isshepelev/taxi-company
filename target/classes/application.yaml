spring:
  datasource:
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5432/taxi
    driverClassName: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create


  mail:
    host: smtp.gmail.com
    port: 587
    username: emailmessage757@gmail.com
    password: wcnf tmux znds szmu
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    sender:
      email: no-reply@example.com
      text: no-reply

  rabbitmq:
    host: localhost
    port: 5672
    username: rabbit
    password: rabbit
    virtual-host: email


#  flyway:
#    url: ${spring.datasource.url}
#    user: ${spring.datasource.username}
#    password: ${spring.datasource.password}